# ============================================================
# industry-chain-research v4.1.1 依赖清单
# 按角色分组;每行带 pin 到 minor 版本(避免破坏性变更)
# Python: 3.10+ (在 3.10/3.11/3.12 验证过)
# ============================================================

# ----- 数据获取 -----
requests>=2.31.0,<3.0
akshare>=1.13.0,<2.0
tushare>=1.4.0,<2.0
duckdb>=0.10.0,<1.0

# ----- 数据处理 -----
pandas>=2.0.0,<3.0
numpy>=1.24.0,<3.0

# ----- 渲染(PIL 用于图像,cairosvg 用于 SVG 转换) -----
Pillow>=10.0.0,<12.0
cairosvg>=2.7.0,<3.0

# ----- 文档/配置 -----
markdown>=3.5.0,<4.0
pyyaml>=6.0.0,<7.0

# ----- 测试(运行时非必需,CI 用) -----
pytest>=7.4.0,<9.0
pytest-cov>=4.1.0,<5.0
ruff>=0.1.0,<1.0
