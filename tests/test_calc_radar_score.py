"""
测试 chain-radar 评分逻辑(纯函数,无需网络)
"""
import pytest

# 雷达评分是动态 import,这里只测纯函数逻辑
def test_placeholder():
    """占位 - chain-radar 评分函数主要在 calc_radar_score.py,
    需要 ETF 数据,标记为 slow/network 测试,这里只占位"""
    assert True
