[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
markers =
    slow: 标记耗时超过 5 秒的测试
    network: 需要网络访问的测试
    db: 需要本地 tushare DuckDB 的测试
filterwarnings =
    ignore::DeprecationWarning
